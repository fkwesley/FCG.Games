apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: fcg-ingress-dev
spec:
  ingressClassName: nginx
  rules:
    - http:
        paths:
          - path: /dev/users-api/?(.*)
            pathType: ImplementationSpecific
            backend:
              service:
                name: fcg-users-svc
                port:
                  number: 80
          - path: /dev/games-api/?(.*)
            pathType: ImplementationSpecific
            backend:
              service:
                name: fcg-games-svc
                port:
                  number: 80
          # - path: /dev/orders
          #   pathType: Prefix
          #   backend:
          #     service:
          #       name: fcg-orders-svc
          #       port:
          #         number: 80
          # - path: /dev/payments
          #   pathType: Prefix
          #   backend:
          #     service:
          #       name: fcg-payments-svc
          #       port:
          #         number: 80